<UserControl
  x:Class="Flow.JiraSearch.Settings.SettingsView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:settings="clr-namespace:Flow.JiraSearch.Settings"
  d:DataContext="{d:DesignInstance Type=settings:SettingsViewModel}"
  d:DesignHeight="450"
  d:DesignWidth="800"
  mc:Ignorable="d"
>
  <Grid Margin="{StaticResource SettingPanelMargin}">
    <Grid.RowDefinitions>
      <RowDefinition Height="auto" />
      <RowDefinition Height="auto" />
      <RowDefinition Height="auto" />
      <RowDefinition Height="auto" />
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto" />
      <ColumnDefinition Width="3*" />
    </Grid.ColumnDefinitions>
    <TextBlock
      Grid.Row="0"
      Grid.Column="0"
      Margin="{StaticResource SettingPanelItemRightTopBottomMargin}"
      VerticalAlignment="Center"
      FontSize="14"
      Text="Base URL"
    />
    <TextBox
      Grid.Row="0"
      Grid.Column="1"
      MaxWidth="600"
      Margin="{StaticResource SettingPanelItemRightTopBottomMargin}"
      HorizontalAlignment="Left"
      VerticalAlignment="Center"
      Text="{Binding Settings.BaseUrl}"
    />
    <TextBlock
      Grid.Row="1"
      Grid.Column="0"
      Margin="{StaticResource SettingPanelItemRightTopBottomMargin}"
      VerticalAlignment="Center"
      FontSize="14"
      Text="API Token"
    />
    <PasswordBox
      x:Name="MaxDecimalPlaces"
      Grid.Row="1"
      Grid.Column="1"
      Margin="{StaticResource SettingPanelItemRightTopBottomMargin}"
      HorizontalAlignment="Left"
      VerticalAlignment="Center"
      Width="600"
      PasswordChanged="TokenBox_PasswordChanged"
    />
    <TextBlock
      Grid.Row="2"
      Grid.Column="0"
      Margin="{StaticResource SettingPanelItemRightTopBottomMargin}"
      VerticalAlignment="Center"
      FontSize="14"
      Text="Maximum Results"
    />
    <TextBox
      Grid.Row="2"
      Grid.Column="1"
      MaxWidth="300"
      Margin="{StaticResource SettingPanelItemRightTopBottomMargin}"
      HorizontalAlignment="Left"
      VerticalAlignment="Center"
      Text="{Binding Settings.MaxResults}"
      PreviewTextInput="NumberValidationTextBox"
    />
    <TextBlock
      Grid.Row="3"
      Grid.Column="0"
      Margin="{StaticResource SettingPanelItemRightTopBottomMargin}"
      VerticalAlignment="Center"
      FontSize="14"
      Text="Default Project (separate multiple with comma)"
    />
    <TextBox
      Grid.Row="3"
      Grid.Column="1"
      MaxWidth="800"
      Margin="{StaticResource SettingPanelItemRightTopBottomMargin}"
      HorizontalAlignment="Left"
      VerticalAlignment="Center"
      Text="{Binding DefaultProjects}"
    />
  </Grid>
</UserControl>
